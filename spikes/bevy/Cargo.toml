[package]
name = "spike-bevy"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = { version = "0.15", features = ["dynamic_linking"] }
game-core = { path = "../../game-core" }
bevy_egui = "0.31"
